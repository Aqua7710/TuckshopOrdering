@model IEnumerable<TuckshopOrdering.Models.Customise>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomiseName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomiseName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomiseID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomiseID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomiseID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
